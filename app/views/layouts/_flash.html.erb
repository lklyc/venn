<div>
  <% if flash.keys.any? %>
    <% flash.keys.each do |key| %>
    <div class="alert alert-<%= key == 'success' ? 'success' : 'danger' %>" role="alert">
      <%= flash[key] %>
    </div>
    <% end %>
  <% end %> 
</div>